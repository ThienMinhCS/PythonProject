services:

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: airline_sqlserver

    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "Airline@2026"

    ports:
      - "1433:1433"

    restart: always